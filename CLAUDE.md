# CLAUDE.md

## 이 프로젝트

개인 기술 블로그. 게임 서버 개발과 만든 것들에 대해 쓴다.
**모바일과 데스크톱 양쪽에서 깔끔해야 한다.** 한쪽을 우선하지 않는다.
(profile-site 는 인스타에서 눌러 들어오는 링크 페이지라 모바일 우선이었다. 여기는 다르다.)

배포는 GitHub Pages, 도메인은 `blog.elkiss.me`.
프로필 페이지(`elkiss.me`)는 별도 저장소(`profile-site`)에 있다.

## 작업 규칙

### push는 사용자만 한다

Claude는 **절대 `git push`를 실행하지 않는다.** 예외 없음.

- 작업이 명확하면 → 커밋 메시지까지 작성해서 **커밋까지만** 한다
- 변경이 복잡하거나 판단이 필요하면 → **staged 상태까지만** 두고 멈춘다
- push는 항상 사용자가 직접 한다

### 커밋은 잘게 쪼개지 않는다

시안을 주고받으며 다듬는 동안에는 파일만 고쳐두고 커밋하지 않는다.
**한 덩어리가 끝났을 때 한 번에** 커밋한다.

서로 상쇄되는 커밋(넣었다 뺀 것, 세 번 고친 문구)이 히스토리에 남을 값어치는 없다.
왜 그렇게 정했는지는 어차피 문서에 적는다.

### 커밋 준비 시 민감 정보를 점검한다

**public 저장소다.** 한 번 push되면 되돌리기 어렵다.
커밋을 준비할 때마다 아래를 확인하고, 걸리는 게 있으면 커밋 전에 알린다.

- 이메일 주소, 비밀번호, API 키, 토큰, 인증서, 개인키
- 실명, 주소, 전화번호
- 로컬 절대 경로 (`D:\...`, `/Users/...`)
- 커밋 작성자 이메일 (`user.email`) — 파일이 아니라 커밋 메타데이터에
  들어가고, public 저장소에서는 그대로 공개된다

### 회사 이야기를 쓸 때

글쓴이는 게임 회사에서 서버를 만든다. **사내 코드와 시스템 설계는 쓸 수 없다.**

- **미발표 프로젝트 이름은 쓰지 않는다.** 회사명까지만
- 사내 시스템의 구조·설계·수치를 그대로 옮기지 않는다
- 쓰고 싶은 주제가 있으면 **일반화한다.** "우리 회사가 이렇게 했다"가 아니라
  "이런 문제는 보통 이렇게 갈린다"로. 회사를 지우고도 남는 것이 글감이다
- 초안에 이 선을 넘는 부분이 있으면 **쓰기 전에 알린다**

### 그 외

- 한 번에 한 단계씩. 시키지 않은 범위까지 미리 만들지 않는다
- 판단이 필요한 결정은 근거를 붙여 문서에 남긴다. 나중에 다시 정하지 않도록
